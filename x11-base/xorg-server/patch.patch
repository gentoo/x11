diff --git a/x11-base/xorg-server/xorg-server-1.8.0.ebuild b/x11-base/xorg-server/xorg-server-1.8.0.ebuild
index fbb1a30..61b78a3 100644
--- a/x11-base/xorg-server/xorg-server-1.8.0.ebuild
+++ b/x11-base/xorg-server/xorg-server-1.8.0.ebuild
@@ -13,7 +13,7 @@ DESCRIPTION="X.Org X servers"
 KEYWORDS="~alpha ~amd64 ~arm ~hppa ~ia64 ~mips ~ppc ~ppc64 ~sh ~sparc ~x86 ~x86-fbsd"
 
 IUSE_SERVERS="dmx kdrive xorg"
-IUSE="${IUSE_SERVERS} hal ipv6 minimal nptl sdl tslib +udev"
+IUSE="${IUSE_SERVERS} doc hal ipv6 minimal nptl tslib +udev"
 RDEPEND=">=app-admin/eselect-opengl-1.0.8
 	dev-libs/openssl
 	media-libs/freetype
@@ -82,6 +82,7 @@ DEPEND="${RDEPEND}
 	>=x11-proto/xineramaproto-1.1.3
 	>=x11-proto/xproto-7.0.13
 	dmx? ( >=x11-proto/dmxproto-2.2.99.1 )
+	doc? ( app-doc/doxygen )
 	!minimal? (
 		>=x11-proto/xf86driproto-2.1.0
 		>=x11-proto/dri2proto-2.1
@@ -109,13 +110,6 @@ pkg_setup() {
 
 	use minimal || ensure_a_server_is_building
 
-	# SDL only available in kdrive build
-	if use kdrive && use sdl; then
-		conf_opts="${conf_opts} --enable-xsdl"
-	else
-		conf_opts="${conf_opts} --disable-xsdl"
-	fi
-
 	# HAL shebang
 	if use hal; then
 		ewarn "Usage of hal is strongly discouraged. Please migrate to udev."
@@ -155,13 +149,15 @@ pkg_setup() {
 		$(use_enable !minimal glx)
 		$(use_enable xorg)
 		$(use_enable nptl glx-tls)
+		$(use_with doc doxygen)
 		${myconf}
 		--sysconfdir=/etc/X11
 		--localstatedir=/var
 		--enable-install-setuid
-		--with-fontdir=/usr/share/fonts
+		--with-fontrootdir=/usr/share/fonts
 		--with-xkb-output=/var/lib/xkb
 		--without-dtrace
+		--with-os-vendor=Gentoo
 		${conf_opts}"
 
 	# (#121394) Causes window corruption
